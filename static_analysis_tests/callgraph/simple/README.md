# Simple Call Graph Test

To get a static call graph in Graphviz DOT format, simply use the included script.
```
./test.sh direct_indirect.wat
```
This will output `call-graph-output.dot` which can be read by Graphviz to generate a visual call graph.

